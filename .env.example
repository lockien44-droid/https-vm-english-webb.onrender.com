# Copy to .env for local dev (never commit .env)
# On Render: Dashboard → Environment → Add Variable
# GEMINI_API_KEYS = key1,key2,key3  (comma-separated, NEW keys only)
GEMINI_API_KEYS=your_new_key_here
